<resources>

    <!-- Base application theme. -->
    <style name="AssignMeTheme.Theme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ShadowBlue</item>
        <item name="colorPrimaryDark">@color/Liberty</item>
        <item name="android:navigationBarColor">@color/ShadowBlue</item>
        <item name="colorAccent">@color/CeruleanFrost</item>
        <item name="android:colorBackground">@color/Alabaster</item>
    </style>

</resources>
